{
  "_comment": "复制为 vault-map.json 后修改（vault-map.json 不入 git）。本示例只含「新用户必须决定」的最小键；其余调优字段（phase_concurrency / KB / env_cleanup / memory_gate / off_peak 等）由代码提供默认值，完整清单见仓库 docs/config-reference.md 或 `otg config show --effective`。",
  "obsidian_vault": "/home/you/Obsidian/MainVault",
  "new_project_root": "/home/you/src",
  "models": {
    "_comment": "必填：assignee 键 → 你的 DSH provider/model 路由（无内置默认，参考 ~/.dsh/settings.yaml 的 provider）。default 为兜底路由。",
    "default": "your-provider/your-model"
  },
  "notifications": {
    "desktop": true
  },
  "max_concurrent_tasks": 0,
  "max_concurrent_tasks_per_project": 2,
  "poll_interval_minutes": 30,
  "agent_server_managed": true,
  "projects": [
    {
      "name": "example-project",
      "path": "/home/you/src/example-project",
      "git_remote": "github.com/you/example-project"
    },
    {
      "_comment": "团队已有项目（如私有 Gitea）：project_type=team 禁止自动建仓/gh 操作；merge_mode 二选一：manual=只推分支人工合并；fork-merge=fork 开发后自动合并进 fork 默认分支。",
      "name": "team-app-fork",
      "path": "/work/team-app-fork",
      "git_remote": "git@gitea.internal.example.com:yourname/team-app-fork.git",
      "project_type": "team",
      "merge_mode": "fork-merge"
    }
  ]
}
